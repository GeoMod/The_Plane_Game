import UIKit
import SpriteKit

struct StartingPositions {
    let level1 = CGPoint(x: 819, y: 181)
    let level2 = CGPoint(x: 800, y: 700)
}

let planePosition = StartingPositions()
planePosition.level1
